; Browser WebRTC controller dialplan
; Endpoint 9999 calls extension 900 in this context to join SECTION01.

[tccs-controller]
exten => 900,1,NoOp(TCCS Browser Controller joining SECTION01)
 same => n,Answer()
 same => n,ConfBridge(SECTION01)
 same => n,Hangup()
